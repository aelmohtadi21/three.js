import { Canvas } from '@react-three/fiber'
import { OrbitControls, Environment, Html } from '@react-three/drei'
import { motion } from 'framer-motion'

function Scene() {
  return (
    <mesh rotation={[0, Math.PI / 4, 0]}>
      <boxGeometry args={[3, 2, 0.2]} />
      <meshStandardMaterial color="#c9a45c" metalness={0.6} roughness={0.3} />
    </mesh>
  )
}

export default function YazaComingSoon3D() {
  return (
    <div className="w-full h-screen bg-black relative">
      <Canvas camera={{ position: [0, 0, 5], fov: 45 }}>
        <ambientLight intensity={0.6} />
        <directionalLight position={[5, 5, 5]} intensity={1.2} />
        <Environment preset="city" />
        <Scene />
        <OrbitControls enableZoom={false} autoRotate autoRotateSpeed={0.6} />
      </Canvas>

      {/* Overlay UI */}
      <div className="absolute inset-0 flex flex-col items-center justify-center text-white pointer-events-none">
        <motion.h1
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 1 }}
          className="text-6xl md:text-7xl font-extrabold tracking-widest"
        >
          YAZA
        </motion.h1>

        <motion.p
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.6 }}
          className="mt-4 tracking-[0.35em] text-sm uppercase text-neutral-300"
        >
          Amazigh · Arab · Identity
        </motion.p>

        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 1 }}
          className="mt-10 text-2xl tracking-widest"
        >
          COMING SOON
        </motion.div>

        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 1.4 }}
          className="absolute bottom-10 text-sm tracking-wide text-neutral-400"
        >
          <p>Instagram · @yaza.official_</p>
          <p>TikTok · @yaza.official</p>
        </motion.div>
      </div>
    </div>
  )
}
